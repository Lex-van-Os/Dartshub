{% extends "layout/widgetless.html.jinja" %} {% block title %}Event Page{%
endblock %} {% block content %}
<div class="row">
  <div class="col">
    {%for event in events %}
    <div class="card mt-4">
      <div class="card-body">
        <h5 class="card-title text-capitalize">{{ event.name }}</h5>
        <p class="card-text">{{ event.date }}</p>
        <a href="/events/location/detail/{{ event.id }}" class="btn btn--dark"
          >Zie details</a
        >
      </div>
    </div>
    {%endfor%}
  </div>
</div>
{% endblock %} {% block script %} {# insert pagesprecific javascript here #} {%
endblock %}
